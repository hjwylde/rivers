<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.hjwylde.rivers.ui.activities.MapsActivity">

    <android.support.design.widget.CoordinatorLayout
        android:id="@+id/content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include layout="@layout/content_maps"/>

        <View
            android:id="@+id/center_marker_anchor"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_gravity="center"/>

        <ImageView
            android:id="@+id/center_marker"
            android:layout_width="@dimen/markerSize"
            android:layout_height="@dimen/markerSize"
            android:layout_gravity="center|top"
            android:tint="@color/accent"
            android:visibility="invisible"
            app:layout_anchor="@id/center_marker_anchor"
            app:layout_anchorGravity="top|center_horizontal"
            app:srcCompat="@drawable/ic_map_marker"
            />

    </android.support.design.widget.CoordinatorLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/fab_margin"
        app:layout_anchor="@id/content_container"
        app:layout_anchorGravity="bottom|end"
        app:srcCompat="@drawable/ic_plus"/>

</android.support.design.widget.CoordinatorLayout>